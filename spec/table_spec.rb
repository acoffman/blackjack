##RSpec test for the "Table" class

#require '../src/table'
#require '../src/card'

#describe Table do

	#before(:all) do
		#@test_table = Table.new(4)
	#end

	#it "should populate the players array with Player object" do
		#@test_table.players.each {|curr| curr.class.is_a?(Player).should == true}
	#end

	#it "should have a dealer as the last Player in the player array" do
		#@test_table.players[@test_table.players.length-1].is_a?(Dealer).should == true
	#end

#end


